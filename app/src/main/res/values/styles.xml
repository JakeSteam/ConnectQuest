<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/city</item>
        <item name="colorPrimaryDark">@color/city</item>
        <item name="colorAccent">@color/black</item>
        <item name="android:navigationBarColor" tools:targetApi="21">@color/green</item>
    </style>

    <style name="AppTheme.Dialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/city</item>
        <item name="colorAccent">@color/blue</item>  <!--Buttons -->
        <item name="android:textColorPrimary">@color/black</item> <!-- Main text -->
    </style>

    <style name="Theme.Transparent" parent="@android:style/Theme.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

</resources>
